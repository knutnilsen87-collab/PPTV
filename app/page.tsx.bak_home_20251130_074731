import Link from "next/link";

type MockVideo = {
  id: number;
  title: string;
  player: string;
  stakes: string;
  length: string;
};

const highlights: MockVideo[] = [
  {
    id: 1,
    title: "High stakes hero call on the river",
    player: "Alex \\"Ice\\" Larsen",
    stakes: "€5/€10 Cash",
    length: "08:42",
  },
  {
    id: 2,
    title: "Final table: sick three-way all-in",
    player: "Mira Santos",
    stakes: "€1k Main Event",
    length: "06:17",
  },
  {
    id: 3,
    title: "Bluff of the week – triple barrel",
    player: "Jin Park",
    stakes: "€2/€5 Cash",
    length: "04:39",
  },
];

const clips: MockVideo[] = [
  {
    id: 4,
    title: "Snap call with pocket deuces",
    player: "Nova",
    stakes: "Tag: Clips",
    length: "00:37",
  },
  {
    id: 5,
    title: "Running it twice vs. the reg",
    player: "GrinderX",
    stakes: "Tag: Clips",
    length: "00:54",
  },
  {
    id: 6,
    title: "Streamer cooler – set over set",
    player: "StreamLab",
    stakes: "Tag: Clips",
    length: "01:12",
  },
  {
    id: 7,
    title: "Slowroll or Hollywood?",
    player: "TBD",
    stakes: "Tag: Clips",
    length: "00:29",
  },
];

export default function HomePage() {
  return (
    <div className="min-h-screen bg-slate-950 text-slate-50">
      <main className="mx-auto flex max-w-6xl flex-col gap-10 px-4 py-10">
        {/* HERO */}
        <section className="grid gap-8 md:grid-cols-[1.6fr,1.1fr]">
          <div className="relative overflow-hidden rounded-3xl border border-slate-800 bg-gradient-to-br from-slate-900 via-slate-900/40 to-slate-900/80 p-8">
            <div className="absolute inset-0 opacity-60 bg-[radial-gradient(circle_at_0%_0%,rgba(250,204,21,0.24),transparent_55%),radial-gradient(circle_at_80%_0%,rgba(56,189,248,0.16),transparent_55%)]" />
            <div className="relative space-y-4">
              <p className="inline-flex items-center gap-2 rounded-full border border-amber-400/30 bg-slate-950/70 px-3 py-1 text-[11px] font-medium uppercase tracking-[0.2em] text-amber-200">
                <span className="h-1.5 w-1.5 rounded-full bg-emerald-400 shadow-[0_0_8px_rgba(52,211,153,0.9)]" />
                ProPokerTV
              </p>

              <h1 className="text-3xl font-semibold leading-tight sm:text-4xl md:text-5xl">
                Claim your seat
                <br />
                <span className="text-slate-200">at the table</span>
              </h1>

              <p className="max-w-xl text-sm text-slate-300/90">
                Entertainment, professionalism and passion for poker — in one universe.
                Real streams and clips will replace these placeholders as the platform evolves.
              </p>

              <div className="flex flex-wrap items-center gap-3 pt-1">
                <Link
                  href="/feed"
                  className="rounded-full bg-gradient-to-r from-amber-300 via-amber-400 to-amber-500 px-5 py-2 text-xs font-semibold text-slate-950 shadow-[0_0_32px_rgba(245,158,11,0.55)] hover:shadow-[0_0_42px_rgba(245,158,11,0.85)] transition-shadow"
                >
                  Watch highlights
                </Link>
                <Link
                  href="/leaderboard"
                  className="rounded-full border border-slate-700 bg-slate-900/80 px-5 py-2 text-xs font-semibold text-slate-200 hover:bg-slate-800/60 transition"
                >
                  View leaderboard
                </Link>
              </div>
            </div>
          </div>

          {/* LIVE TABLE PREVIEW CARD */}
          <div className="flex flex-col gap-4">
            <div className="relative flex-1 overflow-hidden rounded-3xl border border-slate-800 bg-slate-900/80">
              <div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_0%,rgba(251,191,36,0.18),transparent_60%),radial-gradient(circle_at_80%_100%,rgba(59,130,246,0.18),transparent_55%)]" />
              <div className="relative flex h-full flex-col justify-between p-5">
                <div className="flex items-center justify-between text-[11px] text-slate-300">
                  <span className="rounded-full bg-slate-950/80 px-3 py-1 font-medium uppercase tracking-[0.18em]">
                    Live table preview
                  </span>
                  <span className="inline-flex items-center gap-1 rounded-full bg-emerald-500/10 px-2.5 py-1 text-[10px] text-emerald-300 border border-emerald-500/40">
                    <span className="h-1.5 w-1.5 rounded-full bg-emerald-400 animate-pulse" />
                    Status: placeholder
                  </span>
                </div>

                <div className="mt-4 flex flex-1 items-center justify-center">
                  <div className="relative h-32 w-full max-w-sm rounded-2xl border border-amber-300/30 bg-gradient-to-br from-emerald-900/80 via-slate-950 to-slate-900/90">
                    <div className="absolute inset-0 flex items-center justify-center text-[11px] text-amber-100/80 tracking-[0.25em] uppercase">
                      Live video frame placeholder
                    </div>
                  </div>
                </div>

                <div className="mt-4 flex items-center justify-between text-[11px] text-slate-300">
                  <span>Blinds: €5 / €10 • 6-max streamed table</span>
                  <span className="text-slate-400">Overlay, HUD and chat will live here.</span>
                </div>
              </div>
            </div>

            <div className="rounded-2xl border border-slate-800 bg-slate-900/80 px-4 py-3 text-[11px] text-slate-300">
              This is the public home page. Content rails below show how highlights and
              clips can look before real ingestion is wired up.
            </div>
          </div>
        </section>

        {/* HIGHLIGHTS RAIL */}
        <section className="space-y-4">
          <div className="flex items-baseline justify-between gap-4">
            <div>
              <h2 className="text-sm font-semibold tracking-wide text-slate-100">
                Highlights
              </h2>
              <p className="text-xs text-slate-400">
                Wide cards with faux thumbnails – ideal for streams, VODs and hero clips.
              </p>
            </div>
            <Link
              href="/feed"
              className="text-[11px] text-amber-300 hover:text-amber-200 underline underline-offset-4"
            >
              Explore full feed →
            </Link>
          </div>

          <div className="grid gap-4 md:grid-cols-3">
            {highlights.map((video) => (
              <article
                key={video.id}
                className="group flex flex-col overflow-hidden rounded-2xl border border-slate-800 bg-slate-900/80"
              >
                <div className="relative h-32 overflow-hidden">
                  <div className="h-full w-full bg-[radial-gradient(circle_at_0%_0%,rgba(251,191,36,0.35),transparent_60%),radial-gradient(circle_at_100%_100%,rgba(59,130,246,0.4),transparent_60%)] transition-transform duration-500 group-hover:scale-105" />
                  <div className="pointer-events-none absolute inset-0 flex flex-col justify-between p-3 text-[10px] uppercase tracking-[0.18em] text-slate-100/90">
                    <div className="flex items-center justify-between">
                      <span className="rounded-full bg-slate-950/70 px-2 py-1">
                        Thumbnail placeholder
                      </span>
                      <span className="rounded-full bg-black/60 px-2 py-1 text-[9px]">
                        {video.length}
                      </span>
                    </div>
                    <div className="flex items-center justify-between text-[9px] text-slate-200/90">
                      <span>Camera A • Table view</span>
                      <span>ProPokerTV overlay</span>
                    </div>
                  </div>
                </div>

                <div className="flex flex-1 flex-col gap-1 px-4 py-3">
                  <h3 className="text-xs font-semibold text-slate-50 line-clamp-2">
                    {video.title}
                  </h3>
                  <p className="text-[11px] text-slate-400">
                    {video.player} • {video.stakes}
                  </p>
                  <div className="mt-2 flex items-center justify-between text-[10px] text-slate-500">
                    <span>Play of the Week candidate</span>
                    <span className="rounded-full border border-slate-700 px-2 py-0.5 text-[10px]">
                      1.2k views · 234 votes
                    </span>
                  </div>
                </div>
              </article>
            ))}
          </div>
        </section>

        {/* CLIPS RAIL */}
        <section className="space-y-4 pb-10">
          <div className="flex items-baseline justify-between gap-4">
            <div>
              <h2 className="text-sm font-semibold tracking-wide text-slate-100">
                Clips
              </h2>
              <p className="text-xs text-slate-400">
                Compact vertical cards for TikTok / Shorts style moments.
              </p>
            </div>
            <Link
              href="/clips"
              className="text-[11px] text-slate-400 hover:text-slate-200 underline underline-offset-4"
            >
              View all clips →
            </Link>
          </div>

          <div className="grid gap-4 sm:grid-cols-2 md:grid-cols-4">
            {clips.map((clip) => (
              <article
                key={clip.id}
                className="group flex flex-col overflow-hidden rounded-2xl border border-slate-800 bg-slate-900/80"
              >
                <div className="relative h-40">
                  <div className="h-full w-full bg-[radial-gradient(circle_at_0%_0%,rgba(251,191,36,0.3),transparent_55%),radial-gradient(circle_at_100%_100%,rgba(16,185,129,0.45),transparent_55%)] transition-transform duration-500 group-hover:scale-105" />
                  <div className="pointer-events-none absolute inset-0 flex flex-col justify-between p-2 text-[9px] uppercase tracking-[0.18em] text-slate-100/90">
                    <div className="flex items-center justify-between">
                      <span className="rounded-full bg-black/70 px-2 py-1">
                        Clip placeholder
                      </span>
                      <span className="rounded-full bg-black/60 px-2 py-1 text-[9px]">
                        {clip.length}
                      </span>
                    </div>
                    <span className="self-end rounded-full bg-black/60 px-2 py-1 text-[9px]">
                      Vertical 9:16
                    </span>
                  </div>
                </div>

                <div className="flex flex-1 flex-col gap-1 px-3 py-3">
                  <h3 className="text-xs font-semibold text-slate-50 line-clamp-2">
                    {clip.title}
                  </h3>
                  <p className="text-[11px] text-slate-400">{clip.player}</p>
                  <div className="mt-1 flex items-center justify-between text-[10px] text-slate-500">
                    <span>{clip.stakes}</span>
                    <span>⭐ 4.8</span>
                  </div>
                </div>
              </article>
            ))}
          </div>
        </section>
      </main>
    </div>
  );
}